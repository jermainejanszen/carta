import { mode } from "@chakra-ui/theme-tools";

export default {
  global : (props) => ({
    body : {
      scrollbarWidth : "none",
      fontWeight: "thin"
    },
    h1 : {
      fontSize : "3xl",
    },
    h2 : {
      fontSize : "2xl",
    }
  }),
};